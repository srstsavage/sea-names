name: sea-names
channels:
  - conda-forge
dependencies:
  - pytest
  - pytest-cov
  - isort
  - flake8
  - pre-commit
  - types-setuptools
  - mypy
  - black
  - bump2version
  - pip
  - pip:
    - build
    - twine
